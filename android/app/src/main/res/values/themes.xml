<resources>
    <!--
      The visible UI is a Compose Material3 theme (see ui/theme). This XML theme
      only needs to be a valid, action-bar-less app theme for the launch window.
    -->
    <style name="Theme.EdhTracker" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
